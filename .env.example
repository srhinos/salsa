# SALSA Environment Configuration
# Copy this file to .env and customize as needed

# =============================================================================
# REQUIRED: Plex Server Configuration
# =============================================================================

# Use Docker service name if Plex runs in Docker on the same network
PLEX_HOST=plex

# Or use IP address if Plex is on a different host
# PLEX_HOST=192.168.1.100

PLEX_PORT=32400
PLEX_PROTOCOL=http

# =============================================================================
# URLs (for external access)
# =============================================================================

# These URLs must be accessible from your browser.
# Default works for local testing (localhost).
# For remote access, set to your server's IP or domain.

# FastAPI backend URL (port 8001)
BACKEND_URL=http://localhost:8001

# Reflex backend URL for WebSocket (port 8000)
API_URL=http://localhost:8000

# Examples for remote access:
# BACKEND_URL=http://192.168.1.50:8001
# API_URL=http://192.168.1.50:8000

# =============================================================================
# OPTIONAL: Runtime Mode
# =============================================================================

# prod = production mode (default)
# dev = development mode with hot reload
MODE=prod

# =============================================================================
# OPTIONAL: Session Persistence
# =============================================================================

# By default, a random key is generated on each container start.
# This means sessions are invalidated on restart (users must re-login).
#
# To persist sessions across container restarts, set a secret key:
# SECRET_KEY=your-64-character-hex-string
#
# Generate one with: openssl rand -hex 32
